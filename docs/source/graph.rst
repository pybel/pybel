PyBEL
-----

The most useful functions for users are exposed at the top level package. These functions allow for easy import
from URL, file, iterable, or a database.

.. automodule:: pybel
    :members:

Graph
-----

The PyBEL graph is the main interface for accessing the data. It is built on top of a NetworkX MultiDiGraph.

.. autoclass:: pybel.graph.BELGraph
    :members:
